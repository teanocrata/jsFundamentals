const base = 5
const height = 7

const triangleArea = (base, height) => base * height / 2

console.log(`Triangle area with ${base} base and ${height} height id ${triangleArea(base, height)}`)

const radius = 34

const cicleArea = radius => 2 * Math.PI * radius

console.log(`Cirle area with ${radius} radius is ${cicleArea(radius)}`)
